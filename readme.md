# Zorion

**Zorion** — это MMO-песочница с живым самоподдерживающимся миром. Игрок управляет агентом (кораблём, пчелой, персонажем) и влияет на развитие мира через выполнение контрактов. Архитектура универсальна и не привязана к конкретному сеттингу (космос, улей, муравейник, фэнтези).

> **Статус разработки:** MVP в активной разработке. Основные механики генерации вселенной и планет реализованы, ведётся работа над экономикой и производственными цепочками.

---

## 🚀 Быстрый старт

### Требования
- Go 1.21+
- PostgreSQL 15+
- Redis 7+ (для кеширования и брокера)

### Установка и запуск
```bash
# Клонировать репозиторий
git clone https://github.com/lersss/zorion.git
cd zorion

# Настроить переменные окружения
cp .env.example .env
# Отредактируйте .env, указав DATABASE_URL, REDIS_URL и другие параметры

# Применить миграции (выполните SQL-файлы из папки migrations/ в вашей БД)
# Или используйте инструмент миграций, если он есть

# Собрать и запустить
go mod download
go build -o zorion cmd/server/main.go
./zorion

📁 Структура проекта (основные папки)
cmd/server/ — точка входа (main.go).

internal/ — внутренние пакеты:

generator/ — генерация миров, планет, ресурсов, фракций.

handlers/ — HTTP-обработчики (админка, API).

repository/ — работа с БД.

models/ — структуры данных.

names/ — генерация имён.

recipe/ — (в разработке) рецепты и идеальные профили для товаров.

migrations/ — SQL-миграции для PostgreSQL.

web/ — фронтенд (HTML, CSS, JS).

docs/ — документация (см. ниже).

🧪 Текущий статус разработки (10.09.2026)
✅ Реализовано
Генерация вселенной: круглая карта, кластерная структура, проверка minDist, убрана добивка без проверки.

Генерация планет: батчинг, транзакции, ускорение в 5–10 раз.

Архетипы планет: конструктор из климатов, поверхностей, гидросфер, атмосфер и биосфер (~500 комбинаций).

Физическая температура планет: зависит от удалённости от звезды и её светимости.

Модальное окно: визуализация звёздной системы с орбитами, планетами, зумом и мини-картой.

Размер звёзд на карте и в модалке: зависит от спектрального класса.

Генерация имён: латиница, расширенные слоги.

Батчинг для планет: ускорение вставки в БД.

Курсор и подсветка миров на карте.

Клик по планете в модалке: выбор планеты, подсветка, детальная информация с вкладками.

Фильтры на карте: бэкенд-фильтрация по планетам (наличие, жизнь, обитаемость, тип, ресурсы).

Серверная генерация текстур планет: изображения генерируются на бэкенде (Go), отдаются через API.

🔄 В процессе
Мини-карта в модалке — исправление несовпадения вьюпорта (TODO).

Ресурсы — генерация ресурсов с числовыми свойствами, привязанными к типу планеты.

Фракции — создание NPC-фракций на обитаемых планетах.

Экономика — цены, рынки, торговля между мирами.

Контракты — генерация и отображение контрактов.

📅 Планируется
Генерация фракций на обитаемых планетах.

Доска контрактов (генерация, отображение, выполнение).

Экономическая симуляция (рынки, цены, торговля).

Система репутации и уведомлений.

Регионы на карте (визуальное выделение кластеров).

🛠️ Технологический стек
Бэкенд: Go 1.21+

База данных: PostgreSQL 15+ (Event Store, снэпшоты)

Кеш/брокер: Redis 7+

Фронтенд: HTML5, CSS3, Vanilla JS (ES-модули)

Деплой: Docker + Amvera (PaaS)

🤝 Вклад в проект
Проект находится в активной разработке. Если вы хотите внести свой вклад, пожалуйста, ознакомьтесь с документацией в папке docs/ и свяжитесь с автором.

