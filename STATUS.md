# Статус проекта Zorion

> **Единственный источник правды по статусу.** Отвечает на вопросы:
> где мы сейчас, что делаем, что дальше, что сломано.
>
> Не дублирует:
> - `PROMPT.md` — правила работы с ассистентом.
> - `docs/ARCHITECTURE.md` — многопользовательская архитектура, структура кода.
> - `docs/gamedesign/` — дизайн, долгосрочный бэклог.
> - `CHANGELOG.md` — история изменений.

**Последнее обновление:** 2026-09-10
**Ветка:** master
**Хостинг:** Amvera, автодеплой из master
**Общее состояние:** стабильное, в проде

---

## 1. Текущий фокус

**Проект в стабильном состоянии.** Закрыты: описания планет (фазы 1+2), серверная
кластеризация карты, JWT-секрет из env, балансировка классификатора, миграция `000010`,
фикс очистки вселенной (TRUNCATE без CASCADE).

Явного «в работе» сейчас нет — ждём решения по следующей задаче. Кандидаты — в разделе 4.

---

## 2. Что сделано в последних сессиях

### 2.1. Описания планет — фазы 1+2 ✅

- **Фаза 1 (скелет):** 8 типов × (30 openings + 30 closings) = 480 текстов.
- **Ранее:** `gas_giant`, `icy`, `oceanic` — по 150+150.
- **Фаза 2 (Go-код):** 6 файлов в `internal/generator/planet/descriptions_*.go`.
  Автопоиск JSON, вычисление тегов, детерминированный выбор по хэшу, fallback, маппинг.
- Интеграция в `planet_data_generate.go`, `planet_data_gas.go`, `main.go`.
- **Работает в проде:** описания отображаются в карточке планеты, fallback не срабатывает.
- Детали — в `docs/DESCRIPTIONS_WORK.md`.

### 2.2. Серверная кластеризация карты ✅

- `filter_worlds_handler.go` — SQL `GROUP BY` по ячейкам сетки, bounds + cell + gzip.
- Фронт переведён на кластеры: `data.js`, `events.js`, `map_render.js`, `main.js`,
  `animation.js`, `navigation.js`, `map/config.js`.
- `minZoom: 0.02 → 0.001` — можно отдалить до всей галактики.
- **Результат:** карта на 100k миров летает. `total < 100 мс` на запрос.

### 2.3. Безопасность ✅

- **JWT-секрет из env** (`JWT_SECRET`). Хардкод `your-secret-key` убран.
  `InitJWTSecret` в `main.go`, `log.Fatal` при коротком секрете.
- **Проверка алгоритма** в `VerifyToken` (защита от `none`).
- **`.env` убран из git** — был в репозитории, содержал локальный дев-конфиг
  (`127.0.0.1`, `zorion123`, `admin123`). Не критично (прод-секреты в Amvera),
  но почищено. Добавлен в `.gitignore`.
- **`ADMIN_PASSWORD`** — остаётся дефолт `admin123`. В 2do (см. раздел 6).

### 2.4. Балансировка классификатора ✅

- **Ледяная:** `T < 150` теперь требует `ShareOf(Glaciers) >= 30`.
- **Органик:** порог по сумме биосферных форм `>= 25` (раньше — доминанта, что делало
  тип почти недостижимым: 0,1%).
- **Стекло/металл:** пороги `15 → 10`.
- **Статистику после правок ещё не смотрели** — см. раздел 4.

### 2.5. Инфраструктура ✅

- **Миграция `000010`** — `ALTER TABLE assignments ADD COLUMN type, reward`.
- **`GetWorld`** возвращает 404 вместо 500 при недоступных locations/assignments.
- **`compatibility_matrix`** — таблицы в БД нет, но код работает на JSON-дефолтах.
  В 2do (см. раздел 6).

### 2.6. Инцидент и фикс: очистка вселенной ⚠️→✅

**Что было:** кнопка «Очистка вселенной» в админке падала с `upstream request timeout`
на 100k миров. Причина: `DELETE FROM worlds` — долго, Amvera-прокси рвал соединение.

**Первая попытка фикса:** `TRUNCATE worlds CASCADE`. Оказалось **опасно**:
`TRUNCATE CASCADE` работает на уровне **таблиц**, а не строк, и не учитывает
`ON DELETE SET NULL`. Таблица `users` (у неё FK на `worlds`) была снесена целиком
вместе с паролями.

**Итоговый фикс:** `TRUNCATE` **без** CASCADE + явный список зависимых таблиц
+ временное снятие FK у `users`. Всё в одной транзакции. Работает мгновенно,
`users` не трогает.

**Файл:** `internal/handlers/admin_universe.go`, функция `clearUniverseTx`.

**Урок (зафиксирован в `docs/ARCHITECTURE.md`, раздел 4):**
`TRUNCATE ... CASCADE` **не безопасен** там, где есть таблицы, которые нельзя терять,
но они формально ссылаются на очищаемую. Использовать `TRUNCATE` без CASCADE
+ явный список + временное снятие FK.

---

## 3. Известные баги

| # | Что | Где | Приоритет |
|---|-----|-----|-----------|
| B1 | Пагинатор на вкладке «Миры» не работает | `web/static/js/admin/worlds.js` | 🟡 |
| B2 | `Register` возвращает 500 вместо 409 при гонке | `internal/handlers/auth_handlers.go` | 🟡 |
| B3 | Кэш текстур в `planet_image.go` пишется, но не читается | `internal/generator/planet/planet_image.go` | 🟢 |
| B4 | `compatibility_matrix` не создана | миграция / DBeaver | 🟡 |
| B5 | Опечатка `"разряженная"` в `planet_archetypes.json` | `config/planet_archetypes.json` | 🟢 |
| B6 | `ADMIN_PASSWORD` — дефолт `admin123` | `internal/config/config.go` | 🔴 |

---

## 4. Что дальше (ближайшие приоритеты)

Кандидаты на следующую сессию — обсудить и выбрать:

1. **`ADMIN_PASSWORD` → обязательный.** По аналогии с JWT — читать из env,
   `log.Fatal` при пустом. ~15 минут.
2. **`compatibility_matrix`** — создать таблицу миграцией, чтобы админские
   правки сохранялись в БД. ~30 минут.
3. **Проверить статистику классификатора** после правок. Сгенерировать
   1000 планет, посмотреть пропорции: `ледяная` должна быть 12–15%,
   `органик` — 2–5%, `стекло`/`металл` — по 4–5%. ~15 минут.
4. **`Register` 500 → 409** — обработать `unique_violation` (код 23505 в Postgres). ~20 минут.
5. **Расширение описаний** — добить до 150+150 популярные типы
   (`desert`, `volcanic`, `earthlike`). ~5 сессий.
6. **Аномалии как контент** — Go-код, API, детекция на фронте, показ в карточке.
   Библиотека готова (22 файла). См. `docs/gamedesign/08_roadmap.md`.

Полный долгосрочный бэклог — в `docs/gamedesign/08_roadmap.md`.

---

## 5. Схема БД — применённые миграции

Все миграции применяются **вручную** через psql/DBeaver. Файлы в `migrations/`.

| # | Файл | Что | Статус |
|---|------|-----|--------|
| 001 | `001_init.sql` | Основные таблицы | ✅ применено |
| 002 | `002_users.sql` | Пользователи | ✅ применено |
| 003 | `003_add_current_world_to_users.sql` | `current_world_id` | ✅ применено |
| 004 | `004_factions.sql` | Фракции | ✅ применено |
| 005 | `005_economy_tables.sql` | Экономика (пусто) | ⏭ пропущено |
| 006 | `006_add_stellar_params.sql` | Параметры звёзд | ✅ применено |
| 007 | `007_planet_resources.sql` | Ресурсы планет | ✅ применено |
| 008 | `008_add_planet_indexes` | Индексы планет | ✅ применено |
| 009 | `009_compatibility_matrix` | Матрица совместимости | ⚠️ **не применено** (таблицы нет) |
| 010 | `010_assignments_type_reward` | `type`, `reward` в `assignments` | ✅ применено вручную |

**TODO:** применить `009` (создать `compatibility_matrix`).

---

## 6. 2do (отложено)

- **`ADMIN_PASSWORD` из env** — сейчас дефолт `admin123`.
- **`compatibility_matrix`** — таблица не создана, миграция `009` не применена.
- **Опечатка `"разряженная"` → `"разрежённая"`** — синхронно в
  `planet_archetypes.json` и `descriptions_tags.go`.
- **Средние блоки описаний** (`climate`, `surface`, `interior`, `atmosphere`,
  `biosphere`) — пока работаем по формуле `opening + closing`.
- **Ревизия `gas_giant`/`icy`** (600 текстов) — писались до введения тегов
  `has_biosphere`/`no_biosphere`/`inhabited`.
- **CI** — `go vet` + `go test ./...` + `go vet -race`.
- **Имена файлов в разных папках** — сейчас есть коллизии: `web/static/js/config.js`
  и `web/static/js/map/config.js`. Правило зафиксировано в `PROMPT.md`,
  но на фронте пока не применено.

---

## 7. Открытые вопросы

### Для обсуждения (продукт)
- LLM-генерация описаний — когда интегрировать, какой API (Groq/YandexGPT)?
- Мини-игра сбора ресурсов — механика не определена.
- Регионы на карте — визуал или механика?

### Технические
- Кэш статистики планет — когда переходить на SQL-агрегацию при 100k+?

Полный список — в `docs/gamedesign/08_roadmap.md` §8.4.

---

## 8. Связанные документы

- `PROMPT.md` — правила работы с ассистентом.
- `docs/ARCHITECTURE.md` — многопользовательская архитектура, структура кода.
- `docs/DESCRIPTIONS_WORK.md` — рабочий документ по описаниям планет.
- `docs/gamedesign/` — GDD, долгосрочный бэклог.
- `CHANGELOG.md` — история изменений.
- `readme.md` — как запустить проект.

---

## 9. Как обновлять этот файл

**После каждой сессии:**

1. Обновить дату сверху.
2. Переписать раздел 1 («Текущий фокус») — что делаем прямо сейчас.
3. Добавить сессию в раздел 2 («Что сделано»). Не удалять старое — оно
   всё равно уйдёт при следующем релизе в `CHANGELOG.md`.
4. Обновить раздел 3 («Известные баги») — что починили, что появилось.
5. Переписать раздел 4 («Что дальше»).
6. Обновить раздел 5 (миграции) и раздел 6 (2do), если что-то изменилось.
7. Раздел 7 (открытые вопросы) — убрать решённые, добавить новые.

**Никогда не дублировать сюда:**
- Правила работы с ассистентом — они в `PROMPT.md`.
- Многопользовательскую архитектуру — в `docs/ARCHITECTURE.md`.
- Дизайн-концепции — в `docs/gamedesign/`.
- Историю изменений — в `CHANGELOG.md`.