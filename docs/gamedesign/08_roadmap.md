# 08. Дорожная карта и бэклог

**Обновлено:** 2026-09-10

> **Роль документа:** долгосрочный бэклог и открытые вопросы дизайна.
> **Краткосрочный статус** (что сделано, что сейчас, что дальше) — в `STATUS.md`.
> **История изменений** — в `CHANGELOG.md`.

---

## 8.1. Дорожная карта (крупные вехи)

| # | Блок | Статус |
|---|------|--------|
| 1 | Генерация вселенной (Пуассон + кластеры, спектры) | ✅ Сделано |
| 2 | Композиция поверхности и недр (16 форм, 17 типов) | ✅ Сделано |
| 3 | Ресурсы (6 категорий, маппинг форм) | ✅ Сделано |
| 4 | Ядро и физика температуры | ✅ Сделано |
| 5 | Карточка планеты (UI) | ✅ Сделано |
| 6 | Аудит планет (43 правила) | ✅ Сделано |
| 7 | Библиотека аномалий (22 файла) | ✅ Сделано |
| 8 | **Описания планет** (11 типов, ~700 текстов, Go-генератор) | ✅ Сделано |
| 9 | **Серверная кластеризация карты** (100k миров) | ✅ Сделано |
| 10 | **Аномалии как контент** (Go + API + фронт) | 🔄 Следующий блок |
| 11 | LLM-генерация описаний | 📅 Планируется |
| 12 | Тонкая настройка генерации (константы в админку) | 📅 Планируется |
| 13 | Фракции (генерация, войны, контракты) | 📅 Планируется |
| 14 | Экономика (энергия, рынки, производство) | 📅 Планируется |

---

## 8.2. Крупные блоки в разработке

### 8.2.1. Аномалии как контент

Редкие «нарушения» физики — не баги, а фичи мира. Библиотека **готова** (22 JSON-файла
в `config/anomalies/`). Осталось превратить её в рабочий контент:

1. **Go-код:** чтение `config/anomalies/` при старте, кеш в памяти под `RWMutex`.
   Ошибка чтения JSON — `log.Fatal`, а не старт с полупустым кешем.
2. **API:** эндпоинт, отдающий тексты фронту.
3. **Детекция на фронте:** по кодам из аудита (см. `internal/audit/planet/`).
4. **Показ:** отдельный блок в карточке планеты, под описанием.
   Может быть несколько аномалий на одной планете.

Формат и список — в `config/anomalies/README.md`.

### 8.2.2. LLM-генерация описаний

**Идея:** при создании планеты (в `GeneratePlanets`) генерировать уникальные описания
через LLM и сохранять в БД. Детерминизм обеспечен — читается из БД, а не генерится
на каждый запрос.

**API на выбор:**
- **Groq** — быстрый, 1000 запросов/день бесплатно.
- **YandexGPT 5 Lite** — 10 млн токенов/месяц, работает из РФ.
- **Google Gemini** — 30 запросов/день.
- **OpenRouter** — 26+ бесплатных моделей.

**Что генерировать:**
- Обычные описания планет (вместо библиотеки `config/descriptions/`).
- Описания аномалий.

**Fallback:** если API недоступен — библиотека `config/descriptions/` + библиотека аномалий.

**Архитектура:**
- Пакет `internal/llm/` — обёртка над API.
- Вызов из `GeneratePlanetsForWorld` после генерации планеты.
- Батчи по 100 планет с параллельным запросом.
- Поле `description` уже есть в JSON планеты — не надо миграций.

### 8.2.3. Тонкая настройка генерации

Все хардкод-константы вынести в конфиг, редактируемый через админку:
- Спектральные классы (веса, диапазоны T).
- Планеты (количество, возраст, шансы газовых гигантов).
- Классификатор (пороги).
- Физика (альбедо, парниковые факторы).
- Ядро (пороги массы, диапазоны).
- Композиция (множители по T и воде).

**Хранение:** JSON-дефолты + override в БД (как у матрицы совместимости).

---

## 8.3. Долгосрочный бэклог

### 8.3.1. Безопасность

- **`ADMIN_PASSWORD` из env.** Сейчас дефолт `admin123` — такая же дыра, как был JWT.
- **Проверка `unique_violation` в `Register`** — обработать код `23505`,
  вернуть 409 вместо 500.

### 8.3.2. Многопользовательская архитектура

- **Правило проекта:** любая map, доступная из двух горутин, — под мьютексом.
  См. `docs/ARCHITECTURE.md`, раздел 0.
- **Проверить оставшиеся файлы:** `internal/generator/faction/faction.go`,
  `internal/generator/galaxy/galaxy.go` (при параллельной генерации).
- **CI с `go vet -race`.** Автоматически ловит гонки при пуше.
- **Кэш текстур в `planet_image.go`** — пишется, но не читается. При добавлении
  чтения — обязательно под мьютексом.

### 8.3.3. Контент и баланс

- **Проработка каждой формы поверхности** отдельным документом (начиная со «Скал»).
- **Биосферы — проработка интересных.** Что даёт симбиотическая, чем светящаяся
  отличается от растительной.
- **Показ пустой породы в недрах** — решить, показывать или нет.
- **Расширение описаний** до 150+150 популярных типов (`desert`, `volcanic`, `earthlike`).
- **Ревизия `gas_giant`/`icy`** (600 текстов) — писались до введения тегов
  `has_biosphere`/`no_biosphere`/`inhabited`.
- **Экзотические аномалии** — 100+ категорий, по 2–3 на галактику, с совсем
  невозможными условиями. Не в MVP, но в перспективе — отдельная ветка контента.

### 8.3.4. Механики

- **Мини-игра сбора ресурсов.**
- **Регионы на карте.** Визуал или механика?
- **Валюты фракций.**
- **Репутация игрока** с фракциями.
- **Дипломатия между фракциями.**

### 8.3.5. Экономика (расширение)

- **Рецепты товаров.** 20 категорий, идеальные профили.
- **Цены и рынки.**
- **Торговые пути.**
- **Модель энергии** — планетарный рынок, микроконтракты.

### 8.3.6. Фракции (расширение)

- **60+ типов.** Конкретный список.
- **Репутация игрока.**
- **Дипломатия.**

### 8.3.7. Инфраструктура

- **Рефакторинг остальных генераторов имён** на `LocalizedName`.
- **CI** (`.github/workflows/ci.yml`): `go vet` + `go test ./...` + `go vet -race`.
- **Инструменты анализа галактики** (см. 8.5).
- **Вкладка «Совместимость»** в админке (frontend для готового backend).
- **`compatibility_matrix`** — таблица не создана, миграция `009` не применена.
  См. `STATUS.md`, раздел 5.

### 8.3.8. Технические

- **`internal/planet_image.go`** — генерирует текстуры по старому `type`,
  обновить под композицию.
- **`pkg/simulation/batch/`** и **`pkg/worldgen/`** — черновики, не подключены.
- **`internal/core/location_batch.go`** — нерабочий импорт, черновик.

### 8.3.9. Известные баги

Полная таблица — в `STATUS.md`, раздел 3. Здесь — только те, что «живут долго»:

| # | Что | Где | Приоритет |
|---|-----|-----|-----------|
| B1 | Пагинатор на вкладке «Миры» не работает | `web/static/js/admin/worlds.js` | 🟡 |
| B2 | Кэш текстур пишется, но не читается | `internal/generator/planet/planet_image.go` | 🟢 |

---

## 8.4. Открытые вопросы дизайна

### Геймплей
- **Мини-игра сбора** — как играется?
- **Регионы** — визуал или механика?
- **Фракции и сбор** — могут ли запретить игроку собирать на своей территории?
- **Пустая порода в UI** — показывать или нет?

### Экономика
- **Валюты фракций** — когда вводить раздельные?
- **Цена энергии** — как формируется, кто устанавливает?
- **Спрос и предложение** на товары — как моделировать?

### Контент и LLM
- **LLM-API** — Groq, YandexGPT, Gemini?
- **Что делать при отказе LLM** — продолжать, падать или fallback.
- **Фракции: 60+ типов** — какие?
- **Биосферы** — что даёт каждая?
- **Атмосферные ресурсы** — какие газы, с какой скоростью?

### Технические
- **Мини-карта в модалке** — несовпадение вьюпорта.
- **Производительность статистики** при 100k+ планет — когда переходить
  на SQL-агрегацию?
- **Кэш статистики** — когда добавить?

---

## 8.5. Инструменты анализа галактики (черновик)

1. **Карта плотности.** Тепловая карта — где какие типы планет, где жизнь, где ресурсы.
2. **Аналитика по ресурсам.** Каких больше, где дефицит, карта распределения.
3. **Экономическая аналитика.** Цены, дефициты, торговые потоки.
4. **Демографическая.** Распределение населения по фракциям.
5. **Фракционная.** Сила, территории, войны, карта влияния.
6. **Проверка реалистичности.** Автотест на инварианты (уже есть аудит).
7. **Динамика во времени.** Как менялась галактика за тики.
8. **Экспорт.** JSON / CSV / отчёты.
9. **Сравнение галактик.** Какая «здоровая», какая перекошена.
10. **Отладка генератора.** Показать seed → планеты, replay.

---

## 8.6. Как обновлять этот файл

- **Закрыл задачу из 8.2** — перенеси в 8.1, обнови `STATUS.md`, добавь строку в `CHANGELOG.md`.
- **Появилась новая задача** — добавь в 8.2 (если крупная) или 8.3 (если в бэклог).
- **Приоритеты изменились** — перепиши 8.1.
- **Ничего не удаляй** из 8.3 (бэклог) — только перемещай вверх, когда задача становится актуальной.
- **Известные баги** — краткая таблица в 8.3.9, полная — в `STATUS.md` (раздел 3).
- **Открытые вопросы** — в 8.4. Закрыл — убери.
- **Текущий статус, что сейчас делаем** — **не здесь**, а в `STATUS.md`.

---

*Обновлено 2026-09-10.*